import cv2
import requests
import time

print('Bionic-eye Python HLS placeholder script')

# Exemplu de loop simplu
# camera_sources = ['sample1.mp4','sample2.mp4']
# for cam in camera_sources:
#     cap = cv2.VideoCapture(cam)
#     ret, frame = cap.read()
#     if ret:
#         # Detectii placeholder
#         data = {
#             'camera_id': 1,
#             'type': 'person',
#             'confidence': 0.98,
#             'timestamp': time.strftime('%Y-%m-%d %H:%M:%S'),
#             'instance_id': 1
#         }
#         requests.post('http://127.0.0.1:8000/api/detections', json=data)
#     cap.release()
